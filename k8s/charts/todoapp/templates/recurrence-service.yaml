{{/*
TASK-075: Recurrence Service deployment template
*/}}
{{- if .Values.recurrenceService.enabled }}
apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ include "todoapp.fullname" . }}-recurrence-service
  labels:
    {{- include "todoapp.labels" . | nindent 4 }}
    app.kubernetes.io/component: recurrence-service
spec:
  replicas: {{ .Values.recurrenceService.replicaCount }}
  selector:
    matchLabels:
      {{- include "todoapp.selectorLabels" . | nindent 6 }}
      app.kubernetes.io/component: recurrence-service
  template:
    metadata:
      labels:
        {{- include "todoapp.selectorLabels" . | nindent 8 }}
        app.kubernetes.io/component: recurrence-service
      annotations:
        {{- include "todoapp.daprAnnotations" .Values.recurrenceService | nindent 8 }}
    spec:
      containers:
        - name: recurrence-service
          image: {{ include "todoapp.image" (dict "global" .Values.global "service" .Values.recurrenceService) }}
          imagePullPolicy: {{ .Values.recurrenceService.image.pullPolicy }}
          ports:
            - containerPort: {{ .Values.recurrenceService.port }}
          env:
            - name: DATABASE_URL
              valueFrom:
                secretKeyRef:
                  name: {{ include "todoapp.fullname" . }}-secrets
                  key: DATABASE_URL
            - name: DAPR_HTTP_PORT
              value: "3500"
            - name: PUBSUB_NAME
              value: {{ .Values.dapr.pubsub.name | quote }}
            - name: TASK_SERVICE_APP_ID
              value: {{ .Values.taskService.dapr.appId | quote }}
          resources:
            {{- toYaml .Values.recurrenceService.resources | nindent 12 }}
          livenessProbe:
            httpGet:
              path: /health
              port: {{ .Values.recurrenceService.port }}
            initialDelaySeconds: 10
            periodSeconds: 20
          readinessProbe:
            httpGet:
              path: /health
              port: {{ .Values.recurrenceService.port }}
            initialDelaySeconds: 5
            periodSeconds: 10
---
apiVersion: v1
kind: Service
metadata:
  name: {{ include "todoapp.fullname" . }}-recurrence-service
  labels:
    {{- include "todoapp.labels" . | nindent 4 }}
spec:
  selector:
    {{- include "todoapp.selectorLabels" . | nindent 4 }}
    app.kubernetes.io/component: recurrence-service
  ports:
    - port: {{ .Values.recurrenceService.port }}
      targetPort: {{ .Values.recurrenceService.port }}
  type: ClusterIP
{{- end }}
